<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pack.tsoglani.bird.game">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.type.watch" android:required="false"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="pack.tsoglani.bird.game.GameActivity"
            android:label="@string/app_name"
            android:noHistory="true"

            >
        </activity>
        <activity
            android:name="pack.tsoglani.bird.game.MenuActivity"
            android:label="@string/app_name"
            android:noHistory="true"
            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pack.tsoglani.bird.game.UpgradeActivity"
            android:label="Game Upgrades "
            android:noHistory="true" >
        </activity>
        <activity
            android:name="pack.tsoglani.bird.game.UpgradeSelectedItemMenu"
            android:label="Game Upgrades "
            android:noHistory="true" >
        </activity>
        <activity
            android:name="pack.tsoglani.bird.game.MissionActivity"
            android:label="Game Missions "
            android:noHistory="true" >
        </activity>
        <activity
            android:name="pack.tsoglani.bird.game.InfoActivity"
            android:label="Game Information"
            android:noHistory="true" >
        </activity>

    </application>

</manifest>